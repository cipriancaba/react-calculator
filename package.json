{
  "name": "ee-calculator",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "jest-junit": "^5.2.0",
    "react": "^16.5.2",
    "react-dom": "^16.5.2",
    "react-scripts": "2.0.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "testCI": "react-scripts test --ci --reporters=default --reporters=jest-junit --coverage --coverageDirectory test-results/coverage --runInBand",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
